#include <stdio.h>

/**
 * main - check the code
 * program that writes putcharbfollowed by a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	char str[9] = "_putchar";

	puts(str);

	return (0);
}
