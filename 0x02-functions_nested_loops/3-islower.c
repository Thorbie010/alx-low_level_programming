#include "main.h"

/**
 * _islower (int c) - start here
 * program to check if c is a lowercase or uppercase letter
 * Return: nothing
 */
int _islower(int c)
{

	if (_islower(c))
		return (1);
	else
		return (0);
}
