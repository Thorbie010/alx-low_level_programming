#include "main.h"

/**
 * _islower - start here
 * @c: check c
 * program to check if c is a lowercase or uppercase letter
 * Return: 1 or 0
 */
int _islower(int c)
{


	c >= 'a';
	c <= 'z';

	if (c)
		return (1);
	else
		return (0);
}
